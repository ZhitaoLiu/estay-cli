import 'package:flutter/material.dart';
import '../../common/extension/double_extension.dart';
import '../../common/extension/int_extension.dart';

class ES<%= data.name %>Page extends StatefulWidget {

  static const routeName = '/<%= data.underlineName %>';

  @override
  _ES<%= data.name %>PageState createState() => _ES<%= data.name %>PageState();
}

class _ES<%= data.name %>PageState extends State<ES<%= data.name %>Page> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('<%= data.name %>'),
      ),
      body: Container(
        child: Align(
          child: Text('这个<%= data.name %>的页面内容'),
        ),
      ),
    );
  }
}