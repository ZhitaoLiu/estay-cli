import 'package:dio/dio.dart';
import 'package:estay_flutter_template/model/<%= data.underlineName %>_model.dart';

import 'http_request.dart';
import 'http_api.dart';

class <%= data.name %>Request {

  static Future<List<BannerModel>> getTestData() async {
    const params = {
      "client": 3
    };
    // API.xxx
    final result = await HttpRequest.request('xxx', method: 'post', params: params,);

    // json to model
    List<BannerModel> banners = [];
    final bannerArray = result['data'];
    for (var json in bannerArray) {
      banners.add(BannerModel.fromJson(json));
    }

    return banners;
  }

}